# Neon Postgres connection string — enables the shared online leaderboard.
# Copy to .env.local for local dev; on Vercel set it as an environment variable
# (added automatically if you use the Neon integration from the Vercel marketplace).
DATABASE_URL=postgresql://user:password@ep-xxx.region.aws.neon.tech/neondb?sslmode=require
